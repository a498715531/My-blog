<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace= "com.hjj.dao.mapper.TagMapper" >

<select id="findTagsByArticleID" parameterType="long" resultType="com.hjj.dao.pojo.Tag">
    select id,avatar,tag_name as tagName from ms_tag
    where id in
    (select  tag_id from ms_article_tag where  article_id =#{articleid})
</select>
    <select id="findHotsTagIds" resultType="long" parameterType="int">
        SELECT
            tag_id
        FROM
            `ms_article_tag`
        GROUP BY
            ms_article_tag.article_id
        ORDER BY
            COUNT(*)
            DESC limit #{limit}
    </select>
    <select id="findTagsByTagIds" resultType="com.hjj.dao.pojo.Tag"
            parameterType="list">
        select id,tag_name as tagName from ms_tag
       where id in
       <foreach collection="tagIds" item="tagId" separator="," open="(" close=")">
           #{tagId}
       </foreach>

    </select>


</mapper>